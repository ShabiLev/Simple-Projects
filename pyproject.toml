[tool.poetry]
name = "some simple projects"
version = "0.1.0"
description = ""
authors = ["ShabiLev <shabi231@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
imutils = "^0.5.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
